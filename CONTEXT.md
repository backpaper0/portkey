# portkey

サンドボックス化されたコンテナ内で任意個のHTTPサーバーが立ち上がる状況で、単一のゲートウェイポートだけをホストへ公開しておけば、あとはホスト名とポートの組を指定するだけで内部のどのサーバーにも到達できるようにする、単一ポート越しのリバースプロキシ。

## Language

**アップストリーム**:
portkeyがリクエストを転送する先のHTTPサーバー。ホスト名とポートの組で識別される。
_Avoid_: 後続のサーバー、バックエンド、委譲先、内部サーバー

**アップストリーム識別子**:
`<ホスト名>~<ポート>` という文字列表現。1つのアップストリームを一意に指す。
_Avoid_: ホスト〜ポート表記、ターゲット表記、宛先表記

**ゲートウェイポート**:
portkeyプロセス自体が待ち受ける唯一のポート(既定 1541)。すべてのリクエストがこのポートを経由する。アップストリーム識別子に含まれるポート番号(アップストリーム側のポート)とは別概念であり、混同しないこと。
_Avoid_: リッスンポート、公開ポート、待受ポート

**サブドメインルーティング**:
`http://<アップストリーム識別子>.localhost:<ゲートウェイポート>/...` の形式で、アップストリーム識別子をホスト名(サブドメイン)に埋め込むアドレッシング方式。

**パスルーティング**:
`http://localhost:<ゲートウェイポート>/<アップストリーム識別子>/...` の形式で、アップストリーム識別子をパスの先頭セグメントに埋め込むアドレッシング方式。

**コンソール**:
ゲートウェイポートのルート(`/`)でホストされる、使い方の説明とURL生成フォームを提供する画面。
_Avoid_: ヘルプ画面、トップページ
